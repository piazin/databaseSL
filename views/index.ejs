<%- include ('./partials/header'); %>
<body>
    <%- include ('./partials/navbar'); %>
    <div class="container">
        <% documents.forEach(documents => { %>
            <div class="card my-4">
                <div class="card-header">
                    <h3><%= documents.title %></h3>
                </div>
                <div class="card-body">
                    <a href="/<%= documents.slug %>" class="btn btn-dark">Ler Artigo</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
<%- include ('./partials/footer'); %>